{
  "title": "SimplyHired Jobs Scraper",
  "description": "High-performance job scraper for SimplyHired.com. Search by keywords and location. Extracts comprehensive job data including descriptions, salary, company info, and more.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of SimplyHired search URLs to scrape. If provided, these will be used instead of keyword/location search.",
      "editor": "requestListSources"
    },
    "keyword": {
      "title": "Keyword",
      "type": "string",
      "description": "Job search keyword (e.g., 'software engineer', 'data scientist', 'nurse').",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Geographic location for job search (e.g., 'New York, NY', 'Los Angeles', 'Remote').",
      "editor": "textfield",
      "prefill": "USA"
    },
    "results_wanted": {
      "title": "Maximum jobs to scrape",
      "type": "integer",
      "description": "Maximum number of job listings to collect.",
      "minimum": 1,
      "maximum": 5000,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum pages",
      "type": "integer",
      "description": "Maximum number of pagination pages to visit.",
      "minimum": 1,
      "maximum": 100,
      "default": 5,
      "prefill": 5,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Proxy settings. RESIDENTIAL proxies recommended for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  },
  "required": [],
  "additionalProperties": false
}